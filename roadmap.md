# Current tasks

- [ ] Split wired corpus map loading into local-test-only and deploy-safe modules.
- [ ] Add and test the cursor-batched `classify_from_db` action without invoking it.
- [ ] Add the service-only classification results and job-state schema.
- [ ] Add `corpus-classify-driver` on a two-minute schedule and leave it inactive.
- [ ] Run corpus tests, deploy the function, and verify no pipeline run occurred.
