# W24A-V3-TURN — Wave-27 Cohort-Determinism Recurrence Fix

**Dispatch id:** W24A-V3-TURN-2026-07-26
**Controller tick:** 2026-07-26T00:58Z
**Deployed:** 2026-07-26T01:06:58Z (before 02:20:00Z hard cutoff)
**Scope:** deploy-guarded fix on `run-cppa-risk-assessment` ONLY. Discharges
pipeline-state item 96 queued fix for the wave-27 CRITICAL
`qc_r1_4_cohort_determinism` recurrence on doc `7f0de458` (run 140,
quality_run `0e744761`).

---

## Attribution recap (item 96)

- v2 module `risk-w24-turna-v2-2026-07-25` present on all three w27 risk
  docs, no errors — but **all counters 0 on `7f0de458`** while the
  offending strings sat in `scope_and_triggers.scope_notes` and
  `cross_tool_recommendations.cybersecurity_audit_rationale`.
- Root cause: v2 hedge vocabulary matched only `hedge_token + date`
  ("on or around April 1, 2030"). The wave-27 offense used the
  **conditional-parenthetical** variant
  `(applicable if 2027 … — confirm cohort when 2027 revenue is final)`
  and the comparator shape
  `cannot be determined until 2027 … if …; if …`, neither of which
  contains a hedge token immediately adjacent to the resolved date.

## Changes shipped

### `_w24a_v3.ts` (new)

- **Version bump:** `risk-w24-turna-v3-2026-07-26` (stamp
  `w24a-v3@2026-07-26T01:15:00Z`).
- **Walker coverage (a):** `strings_scanned` now counts every non-anchor
  string walked (v2 counted only B1-ext strings). ANCHOR_KEYS unchanged.
  Reserved subtrees (`_meta`, keys prefixed `_`) preserved verbatim.
- **Resolved-cohort anchor (b.i):** matches every resolved cohort
  variant (`April 1, 2029` / `April 1, 2030` / `Apr 1 …` / `1 April …` /
  `2029-04-01` / `2030-04-01`). `cohort_resolved` now fires on
  detection, not only on rewrite; `cohort_resolved_near_cite` fires
  when the § 7121(a) cite is co-present.
- **Hedge sentence excision (b.ii):** whole-sentence excision (no
  partial-clause splicing) when a sentence contains BOTH a hedge
  trigger AND a cohort reference (resolved date, § 7121 cite, or the
  standalone word "cohort"). Hedge triggers extended to:
  - `confirm(ed) cohort when`
  - `cannot be determined until`
  - `applicable if 2027`
  - `if 2027 (annual gross) revenue`
  - `should be confirmed when`
  - `when 2027 revenue is final`
  - `the cohort determination should be confirmed`
  - `if triggered`
  - `would fall in the`
- **Deadline audit preserved:** § 7121(a) cohort entries in
  `cross_tool_recommendations` (array OR object shape) are checked
  against the deterministic `2029-04-01` / `2030-04-01` literals;
  anchor field NEVER mutated.
- **Doctrine:** fail-open at every helper AND at the orchestrator
  (`errors` counter), idempotent (second pass no-op), deterministic
  (byte-identical output across invocations).

### `_w24a_v3.test.ts` (new — 11 tests, all green)

Regression fixtures pinned VERBATIM from `quality_run_documents` for
doc `7f0de458` (read from DB, not retyped):

1. `SCOPE_NOTES_7F0DE458` — full `scope_and_triggers.scope_notes` string
   with the parenthetical hedge.
2. `CTR_CYBER_RATIONALE_7F0DE458` — full
   `cross_tool_recommendations.cybersecurity_audit_rationale` string
   with the comparator conditional.

Coverage includes:
- **Attribution proof:** applying v2 (`applyW24RiskTurnA`) to the
  fixtures leaves the parenthetical hedge in place and
  `cohort_resolved` stays 0 — proving v2 missed the wave-27 defect.
- Walker coverage tests for both offending subtrees.
- Idempotency, determinism, anchor immunity, `_meta` preservation,
  whole-sentence excision (no partial residue), resolved-date
  detection without hedge.

### `index.ts` (wire)

- `BUILD_STAMP` bumped: `t7-risk-pilotfix@2026-07-25T22:32:00Z` →
  `w24a-v3@2026-07-26T01:15:00Z`.
- Boot log echoes all prior stage stamps unchanged (w23, w24 turnA,
  w24a-v3, t7-risk-pilotfix, build_stamp).
- v3 module wired AFTER v2 turnA and BEFORE the LEAK-PREV P1 emit
  gate. Merges telemetry onto the existing `_meta.internal.risk_w24a`
  slot with `version` bumped and `v2_stamp` preserved for lineage.

## Deploy guards

Pre-deploy snapshot at 2026-07-26T00:58Z: `quality_batch_runs` 0,
`quality_runs` 0, `cppa_assessments` in-flight 7 — all seven are stale
`pending` rows aged 4–14 days (oldest 2026-07-11), no active
generation. Effective 0/0/0. Deployed 2026-07-26T01:06:58Z, well
before the 2026-07-26T02:20:00Z hard cutoff.

## Out of scope this turn

- Truncated-citation "Cal. Civ. 105(d)" class (queued Class-A
  citation-audit sibling — separate turn).
- T7 opening surfaces (`risk_t7_opening` module untouched;
  T7-PILOT-FIX-2 is separate).
- All other edge functions; no CPPA generator other than
  `run-cppa-risk-assessment`.

## Gate

W24A-v3 must land + read clean on the following wave (wave-28) before
the cohort class is called fixed. T7 step-2 (admt) remains HELD
pending T7-PILOT-FIX-2 + CEO checkpoint.

## Test output (28/28 green: v2 turnA + v3)

```
running 11 tests from ./run-cppa-risk-assessment/_w24a_v3.test.ts
W24A-V3: stamp + version shape ... ok
W24A-V3 [attribution]: v2 (turnA) does NOT scrub the doc-7f0de458 scope_notes parenthetical hedge ... ok
W24A-V3 [walker coverage]: reaches scope_and_triggers.scope_notes (fixture doc 7f0de458 verbatim) ... ok
W24A-V3 [walker coverage]: reaches cross_tool_recommendations.cybersecurity_audit_rationale (fixture doc 7f0de458 verbatim) ... ok
W24A-V3: idempotent — second pass no-op on excision counters ... ok
W24A-V3: deterministic — byte-identical output across two invocations ... ok
W24A-V3: anchor keys (deadline, citation, provision) NEVER mutated ... ok
W24A-V3: reserved subtree _meta preserved verbatim ... ok
W24A-V3: whole-sentence excision — no partial-clause residue ... ok
W24A-V3: unrelated prose untouched; strings_scanned reflects walker coverage ... ok
W24A-V3: resolved cohort date detection fires even without hedge ... ok

ok | 28 passed | 0 failed
```
