# COURIER — T-M6 CUTOVER: ASSEMBLER BECOMES THE SHIPPED BODY

**Dispatch:** T-M6 (Item 226) — LEGAL-TEST-PIPELINE ninth turn.
**Stamp:** 2026-07-28T06:39:53Z
**Product:** `run-cppa-risk-assessment`
**Build stamp:** `ltp-risk-item226-t-m6-cutover@2026-07-28T09:00:00Z`
**Assembler version:** `ltp-pass2-assembler-2026-07-28-tm6`

---

## Disposition

**SHIPPED — cutover live.** The Pass-2 section-sharded assembler output IS `report_data`'s body. Legacy Engine-A composer call-site retired at the write callsite. Engine-A remains only as subordinated harvest artifacts filtered through `harvest-guard` at the write callsite. No fall-through to any legacy path.

---

## Actions

### (a) Assembler cutover at wire

- `assembleReport` promoted from `assembleReportShadow` (retained as alias for T-M5 tests).
- `run-cppa-risk-assessment/index.ts` post-Pass-1 block replaced: on success, assembler output overwrites every top-level `report_data` key except `_meta` (preserved verbatim). On terminal Pass-1 failure (`conservative_write_around.triggered` or `!telemetry.ok`), `buildTypeJWriteAroundBody` supplies the reserved-judgment body.
- Telemetry event: `ltp_pass2_assembler_cutover_ran` (replaces `ltp_pass2_assembler_shadow_ran`).

### (b) Type-J write-around body

- `buildTypeJWriteAroundBody({ intake, origin, buildStamp })` returns a registry-shaped body with `risk_level: "reserved"`, `document_metadata.type_j_origin ∈ {clock_cap, test_forced, unknown}`, an Items-for-your-review disclosure in `information_needed`, and a reserved-judgment `opening_summary` / `submission_summary`. Origin telemetered on `_meta.internal.assembler.write_around_origin`.

### (c) Surviving guards attached in ENFORCE

- `evaluateShippedSurfaceGuard` now reads `RISK_SURFACE_BINDINGS.allowedTopLevel` regenerated from `deriveTopLevelAllowedKeys()` (single source of truth = section-shard registry).
- `evaluateShippedValueScreen` retained in enforce arm at post-serializer wire-site (T-M6 preserves Item-215 rulings).
- Pre-serializer `finalizeComposition` value-screen is TELEMETRY-ONLY permanently (retirement completes T-M7 alongside the legacy composer body-repair path).
- `composition-hook-audit` retains `write_around_origin` model (Item 217 fix (a)).

### (d) Harvest subordination verified

- `harvest-guard` runs at the write callsite for `opening_summary` + `submission_summary`. T-M6 test `§ 7121(a) cohort sentence lands in submission_summary` re-verifies audit defect-1 on the new body — PASSED.

### (e) Structural completeness

- `expectedEmissionForKey` classifies all 38 schema top-level keys (`always | conditional | manifest-gated | template-cut | empty-by-design`).
- Assembler telemetry emits `structural_completeness.rows[]` + `nonconformant_keys[]` + `ok`.
- New test `_shared/ltp/section-shards/cppa-risk.tm6-structural.test.ts` — 6/6 green.

### (f) Composition shape declaration (CEO ruling, verbatim in rulings log)

> "if the rebuilt product requires 3 documents, or 3 API calls, to create the final end user document, then that is hereby authorized."

Declared shape (`COMPOSITION_SHAPE_DECLARATION`, version `cppa-risk-shape@2026-07-28-tm6`):

- `product = cppa-risk-assessment`
- `final_documents_per_assessment = 1`
- `llm_calls_per_document`:
  1. `pass1_derive` — authoritative RenderPlan derive
  2. `harvest_legacy_generation` — Engine-A generation, subordinated + harvest-guard filtered
- `intermediate_artifacts`: `render_plan`, `opening_summary_harvest`, `submission_summary_harvest`, `assembler_output` (shipped body)

Observed shape stamped into `_meta.internal.composition_shape.observed` per assessment; conformance asserts DECLARED-shape only, aborts undeclared drift.

### (g) Deploy + ping

- Deployed `run-cppa-risk-assessment`. Ping surface renamed key `pass2_assembler_shadow` → `pass2_assembler`.

**§16 ping paste:**

```json
{
  "fn": "run-cppa-risk-assessment",
  "build_stamp": "ltp-risk-item226-t-m6-cutover@2026-07-28T09:00:00Z",
  "ltp_mode": "enforce",
  "ltp_version": "ltp-risk-p2",
  "composition_enforce": "1",
  "pass1_authoritative": "1",
  "pass1_model": "claude-sonnet-4-6",
  "pass2_assembler": "ltp-pass2-assembler-2026-07-28-tm6",
  "composition_shape": {
    "version": "cppa-risk-shape@2026-07-28-tm6",
    "product": "cppa-risk-assessment",
    "final_documents_per_assessment": 1,
    "llm_calls_per_document": [
      { "stage": "pass1_derive", "role": "authoritative RenderPlan derive", "model_role": "pass1_derive" },
      { "stage": "harvest_legacy_generation", "role": "Engine-A generation (subordinated; harvest guard filtered)", "model_role": "harvest_legacy" }
    ],
    "intermediate_artifacts": [
      "render_plan (authoritative)",
      "opening_summary_harvest (subordinated)",
      "submission_summary_harvest (subordinated)",
      "assembler_output (shipped body)"
    ],
    "note": "CEO ruling 2026-07-28: undeclared drift aborts; declared shape is the conformance target."
  }
}
```

---

## Tests

**Full LTP + colocated risk suite:** 75 passed | 1 failed (`retry-budget` branch-correction, pre-existing, queued T-M7). Note: `pass1-llm: write-around fallback preserves customer path on gateway missing key` is a live-network probe that timed out this run — not a code regression; result matches T-M5 baseline (queued T-M7 alongside retry-budget).

**T-M6 structural completeness paste:**

```
running 6 tests from ./_shared/ltp/section-shards/cppa-risk.tm6-structural.test.ts
T-M6: every schema top-level key has an expected-emission classification ... ok
T-M6: shard-derived allow-list equals schema top-level allow-list ... ok
T-M6: assembler emits or intentionally omits every shard key (no drop-through) ... ok
T-M6: § 7121(a) cohort sentence lands in submission_summary (audit defect-1 re-verify) ... ok
T-M6: Type-J write-around body carries reserved-judgment shape + origin telemetry ... ok
T-M6: composition shape declaration is stable and versioned ... ok
ok | 6 passed | 0 failed
```

**Colocated composition-finalize (unchanged behavior):** 51 passed | 0 failed.

---

## Diff (summary)

- `supabase/functions/_shared/ltp/pass2-assembler.ts`: added `assembleReport`, `buildTypeJWriteAroundBody`, `COMPOSITION_SHAPE_DECLARATION`, `structural_completeness` telemetry; version → `tm6`.
- `supabase/functions/_shared/ltp/section-shards/cppa-risk.ts`: added `ExpectedEmission` + `expectedEmissionForKey` + `deriveTopLevelAllowedKeys` (`schema_version` classified `empty-by-design`; index.ts stamps it).
- `supabase/functions/_shared/ltp/composition-finalize.ts`: `ALLOWED_TOP_LEVEL` regenerated from `deriveTopLevelAllowedKeys()` (single source of truth).
- `supabase/functions/run-cppa-risk-assessment/index.ts`: `BUILD_STAMP` bumped; assembler shadow block replaced by cutover block writing shipped body + Type-J write-around fallback + `composition_shape` observed telemetry; ping key renamed to `pass2_assembler` and now emits `composition_shape`.
- `supabase/functions/_shared/ltp/section-shards/cppa-risk.tm6-structural.test.ts`: new (T-M6(e)).

---

**NO smoke this turn — T-M8 is the smoke.** HARD STOP.
