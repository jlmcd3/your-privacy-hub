# WAVE-21 DIGEST — Courier Report (EXTRACTED)

**Stamp:** 2026-07-25T11:34:34Z (sandbox clock re-read immediately before write)
**Dispatch:** `WAVE21-DIGEST-EXTRACTED` (DOCS-ONLY; supersedes the 11:28:43Z first-cut record)
**Scope confinement:** this file + `docs/pipeline-state.md` (item 47 rewrite + §5 "Wave 21" block + "Last updated" header). No code, no deploys, no edge-function/rubric/grader/golden/registry/fixture edits, no DB writes.

---

## 1. Batch identity

- **Campaign:** `fd1be147`
- **Batch:** `12640b0f-abd4-4944-9c10-3a1ab108b3f7`
- **Launched:** 2026-07-25T11:00:04Z
- **Complete:** 2026-07-25T11:22:28Z (~22.4 min); phase=done, no `last_error`, clean orchestrator finish.
- **INSTRUMENT OF RECORD:** `gc-2026-07-25-s4-eu-uk-ca-au-sg` — **FIRST s4 WAVE**. All three `quality_runs` rows carry `grader_context_version=s4`. Grader-map correction item 46 deployed 10:52:17Z, >20 min pre-launch; timing guard held.

## 2. Scores

Format: tool / ours / GPT / dims acc-cite-hall-ana-int-fmt / checks.

| Tool | Run | Quality-run | Ours | GPT | Dims | Checks |
| --- | --- | --- | --- | --- | --- | --- |
| cppa-admt  | 110 | `991b2fda` | **84.05** | 88 | 88-77-85-86-85-87 | 118/136 (4 docs pooled; run-row `batch_size=2` vs 4 docs — known counter quirk, noted again) |
| cppa-risk  | 134 | `3549f347` | **79.35** | 86 | 80-79-79-79-80-86 | 64/82 |
| cppa-cyber | 114 | `3c573f4b` | **87.15** | 89 | 88-87-90-82-84-89 | 47/58 |

No gate_v2 pass (no tool has all dims ≥90). Certification counters remain **0/3** on the s4 instrument. Survivors: admt 1 CRITICAL + 10 high; risk 0 critical + 12 high; cyber 0 critical + 5 high.

## 3. ADMT trajectory callout (CEO standing order)

`83.75 → 80.50 → 79.40` (all s3) →‖ `84.05` (s4).

**CROSS-INSTRUMENT BREAK at wave 21.** Per noise-floor rule, s3↔s4 scores are NOT comparable as a trajectory — s4 removed false-positive § 7150(b)(3)/§ 7121(a)(3) penalties, so an unknown share of the +4.65 is instrument correction, not product improvement. **Same-instrument admt trajectory RESTARTS at wave-21 baseline 84.05.**

## 4. W20 TURN A efficacy (`w20-admt-turna@2026-07-25T09:36:35Z`)

- **B1 (variant-splice scrub):** SUPPORTIVE — zero splice/fusion findings (wave-20 `e337fb5d` class absent).
- **B4:** SUPPORTIVE — no empty-`information_needed` findings on admt.
- **B2 (fallback-density):** **INSUFFICIENT** — fallback-density remains the sole CRITICAL (doc `3746fd24`, `rubric_invented_admt_section`): pervasive "the applicable ADMT-subchapter provision" including contexts where a verified pinpoint exists.

Other admt survivors:

- `e6_counsel_referral` ×3 on doc `34115ae8` (body-text "your Privacy Officer should…" directives).
- Empty citation fields on `opt_out_gaps` entries (`34115ae8`, `rubric_citation_misapplied`).
- § 7155(a)(1) submission-vs-timing misapplication in `deadline_table` (`731689ba`).
- `h6_admt_governing_anchor` ×2 — § 7001(e)(1)/§ 7001(ddd) cited as sole governing anchors for action duties (`731689ba`).
- § 7150(b)(3) cited for a sell/share-documentation proposition (`3746fd24`) — **NOTE:** the s4 instrument is behaving CORRECTLY here (verifies (b)(3) for the ADMT trigger, flags misapplication to a different proposition).
- Intake-supported 45-day timeline written around instead of used (`d98f46e3`).

## 5. W20 TURN B efficacy (`w20-risk-turnb@2026-07-25T09:48:30Z`)

Stamp CONFIRMED in doc telemetry `_meta.internal.risk_w20b`. Reconciliation-suppression not contradicted, but the **FACT-LEDGER CLASS RECURS HARD** and risk is now the weakest CPPA tool at 79.35 (≈flat at campaign floor).

- Doc `ccec6376` FABRICATES intake values:
  - `sensitive_location_basis` asserted "Yes — systematic observation of workers/students/applicants" where intake says "Not applicable — no sensitive-location processing".
  - Vendor adverse-effects asserted despite `q5_sell_share=No`.
  - Fabricated § 7150(b)(5) `inconsistency_flags` conflict.
- Doc `283f8c11`:
  - Asserts "record confirms profiling" contrary to `q5b_profiling_observation`.
  - Invents field names ("ADMT-logic/ADMT-description/ADMT-opt-out all record n/a").
  - `rubric_internal_reasoning_leak` ×2 — "the trigger review — established on the record" fragments in `scope_notes`; "the cyber-audit tier review" module-name leak in `cross_tool_recommendations`.
- `information_needed` self-contradiction class ×2 (`7f8bc403`, `ccec6376`) — asks for pinpoints the report itself already provides/negates.

**Next risk fix turn must fold:** fact-ledger ENFORCEMENT gap (claims contrary to ledger values, not just missing support), `scope_notes` fragment leak scrub, `information_needed` contradiction filter, plus queued **RISK-B1-COHORT-EMITTER** (§ 7121(a)(3), approved corpus row `cppa-7121`).

## 6. Cyber

87.15 best-of-wave. Drivers:

- § 7123(c)(N) subsection-map misapplications (`2355ea77`).
- Unsupported "patient health information" categories (`2355ea77`).
- Derived-arithmetic claim presented from intake (`85478f8e`).
- § 7122(g) retention-nuance misapplication (`85478f8e`).
- Garbled splice "SailPoint provides comparative guidance" (`49353ce0` — splice class, cyber variant).

## 7. Monitoring flag — ADMT stamp telemetry gap

Wave-21 admt docs carry **NO** `_w19_admt_turna`/`_w20_admt_turna` keys under `_meta.internal` (`risk_w20b` present on risk docs). Most likely the LEAK-PREV-P2 whitelist serializer drops unregistered internal keys. **Next admt turn:** register a build-stamp echo key in the serializer whitelist so digests can confirm build-of-record from doc telemetry. Do NOT change anything now.

## 8. Deploy locks / queue

- Batch terminal 11:22:28Z — **between-waves window OPEN**.
- Queue per plan:
  1. **DPIA-REGISTRY-WIRING** deploy turn — after CEO courier review.
  2. **RISK-B1-COHORT-EMITTER** — folded into the next risk fix turn.

## 9. Guardrails observed

- Docs-only; writes limited to this file and `docs/pipeline-state.md`.
- No code, no deploys, no edge-function/rubric/grader/golden/registry/fixture edits, no DB writes.
- Every timestamp re-read from the sandbox clock immediately before write (dispatch stamp: 2026-07-25T11:34:34Z).
- Facts transcribed faithfully from the monitoring-agent extract; not re-derived.
- Scope-clean diff: two docs files only (this courier + `docs/pipeline-state.md`).
