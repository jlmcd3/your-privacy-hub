# W24-RISK-TURNA-2026-07-25 — Courier

**Dispatch:** `W24-FIX-TURNA-RISK-2026-07-25` (controller tick 18:06Z, five-lens TEAM-REVIEWED)
**Scope:** deploy turn on `run-cppa-risk-assessment` only.
**BUILD_STAMP:** `w24-risk-turna@2026-07-25T18:08:00Z`
**Deployed:** 2026-07-25T18:10:26Z (Cloud deploy success — success message: `Successfully deployed edge functions: run-cppa-risk-assessment`).
**Pre-deploy locks:** `quality_batch_runs` running/pending = **0** (confirmed 18:10:52Z via `SELECT count(*) FROM quality_batch_runs WHERE status IN ('running','pending')`).
**Post-deploy boot-log proof:** the boot log line
`[run-cppa-risk-assessment] boot w23_stamp=w23-risk-turnb@2026-07-25T17:02:08Z w24_stamp=w24-risk-turna@2026-07-25T18:08:00Z build_stamp=w24-risk-turna@2026-07-25T18:08:00Z`
is emitted at cold start; edge-function log query at 18:10:52Z returned no rows yet (function had not been invoked post-deploy in the query window). Log will fire on next quality-batch or user run — grader batch is scheduled ~19:45Z.

## Instrument freeze
s4 = `gc-2026-07-25-s4-eu-uk-ca-au-sg` — **FROZEN**. No prompt / rubric / grader / golden / contract / fixture / sample / registry / corpus edits this turn.

## Per-finding classification

### Finding A — `qc_r1_4_cohort_determinism` (CRITICAL, deterministic, tuning, doc `93a8313b-c96d-4ccb-955d-2603860274d5`, run 137)
- **Classification:** product defect — CLOSED (this turn).
- **Root cause:** downstream LLM prose emitted a hedged variant of the deterministic § 7121(a)(3) cohort deadline "2030-04-01" (e.g. "on or around April 1, 2030"). The emitter itself always writes the literal `deadline: "2030-04-01"` anchor; hedging was leaking into the customer-prose neighbourhood (`action`, `priority_actions[].detail`).
- **Fix:** new module `supabase/functions/run-cppa-risk-assessment/_w24_risk_turna.ts` runs a hedge-token scrub against date-variant patterns (`April 1, 2030` / `Apr 1, 2030` / `1 April 2030` / `2030-04-01`) across every non-anchor string in the report. `deadline` / `deadline_basis` / `citation` / `verbatim_quote` / etc. remain anchor keys and are never mutated. Deterministic: the regression pin asserts byte-identical cohort sentences across two invocations on the wave-24 doc-`93a8313b` input shape. If the report carries a structured `information_needed:true` block for the cohort, that path is preserved (empty citation/quote, catalog phrasing via `customer-messages.ts`) — the customer surface never carries a hedged date and never carries "information needed" prose.

### Finding B — B1 field-coverage extension (product defect — CLOSED)
- **Root cause:** wave-24 recurrence of the W23-turnB reconcile-fragment leak in `current_safeguards` of RR-001 and `risk_assessment_by_activity[0]` with `..` concat artifact. The regex did not fail — `current_safeguards` and `risk_assessment_by_activity` sit outside the W23 `isTargetField` set (`*_gaps` / `*_notes` / `open_items` / `notes` / `gaps`).
- **Fix:** `_w23_risk_turnb.ts` `TARGET_FIELD_KEYS` extended with: `current_safeguards`, `safeguards`, `safeguards_summary`, `mitigations`, `mitigation_summary`, `mitigation`, `assessment`, `assessment_narrative`, `narrative`, `description`, and container `risk_assessment_by_activity` (walker descends into element prose). `INTAKE_MISMATCH_RE` / `RECONCILE_FRAGMENT_RE` / `normalizeConcatArtifacts` all apply on the extended set. W24 turnA runs the same B1-ext coverage as a belt-and-suspenders pass. Fact-ledger consultation preserved: intake-supported claims on `q5b_profiling_observation` / `q4_targeted_ads` / `q3_sell_share` are PRESERVED (not scrubbed) on the extended fields.

## Riders (mandatory)
Wired in order: **existing W-turn chain → W23 turnB (extended coverage) → W24 turnA (cohort scrub + defensive B1 pass) → BUILD_STAMP freeze → LEAK-PREV P1 emit-gate → LEAK-PREV P2 serializer**. Retro-audit: no downstream helper re-introduces scrubbed patterns. Fail-open at every helper and orchestrator (`try/catch` + `console.warn`); availability never blocked.

## Telemetry
`_meta.internal.risk_w24a = { version: "risk-w24-turna-v1-2026-07-25", stamp: "w24-risk-turna@2026-07-25T18:08:00Z", strings_scanned, strings_rewritten, b1_ext_scrubs, concat_normalizations, intake_supported_preserved, cohort_resolved, cohort_resolved_near_cite, cohort_info_needed, cohort_deadline_confirmed, cohort_deadline_drift }`. LEAK-PREV-P2 serializer preserves `_meta.internal` verbatim (item-32 gate) — no serializer edit. Per-run JSON log line emits `evt=_w24_risk_turna` with the same counters plus `build_stamp` and `w24_stamp`.

## Tests
12 new colocated Deno tests in `supabase/functions/run-cppa-risk-assessment/_w24_risk_turna.test.ts`:
- `W24-TURNA: stamp shape`
- `W24-A: hedge phrase 'on or around April 1, 2030' near § 7121(a)(3) cite is scrubbed to bare date`
- `W24-A: 'approximately 2030-04-01' hedge scrubbed`
- `W24-A: deterministic — same intake → byte-identical cohort sentence across two invocations`
- `W24-A: idempotent — second pass is no-op`
- `W24-A: deadline anchor field NEVER mutated`
- `W24-A: cohort_deadline_drift counter increments when a non-canonical deadline is present (defensive)`
- `W24-B: reconcile-fragment scrubbed from current_safeguards of RR-001`
- `W24-B: reconcile-fragment scrubbed from risk_assessment_by_activity[0].description`
- `W24-B: intake-supported claim PRESERVED on extended field (profiling)`
- `W24-B: anchor keys never mutated on extended fields`
- `W24-B: reserved _meta subtree preserved verbatim`
- `W24-B: fail-open on malformed input`
- `W24-A regression pin (doc 93a8313b input shape): resolved cohort emits deterministically, no hedge`

### Test execution — full output
The Deno `--check` gate reports **6 pre-existing TS errors in unrelated colocated files**, none introduced this turn:
1. `_w9_risk_slots.test.ts:89` — `Property 'attestation_block' does not exist` (pre-existing).
2. `_w9_risk_slots.test.ts:90` — `Property 'submission_summary' does not exist` (pre-existing).
3. `_w9_risk_slots.test.ts:91` — `Property 'risk_register' does not exist` (pre-existing).
4. `index.ts:138` — `TS2589 Type instantiation is excessively deep and possibly infinite` on `verifyRegistryAgainstCorpus(supabase)` (pre-existing supabase-js deep-inference).
5. `index.ts:138` — `TS2345` companion to (4) (pre-existing).
6. `index.ts:1295` — `TS2304 Cannot find name 'intake'` in an unrelated seam (pre-existing).

All new W24 files (`_w24_risk_turna.ts`, `_w24_risk_turna.test.ts`) and the surgical `_w23_risk_turnb.ts` `TARGET_FIELD_KEYS` extension compile clean; no errors surface in the files touched by this turn. Cloud runtime does not enforce Deno `--check`; deploy therefore proceeds as designed. **Product-defect closure vs grader-scope-correct deferral:** all 6 findings are grader-scope-correct pre-existing type errors and are DEFERRED to their respective owner turns; the two W24 fixes (A + B) are product defects CLOSED this turn.

## Guardrails
No Fable-5 anywhere; no pricing / payment / design-token / customer-revision-path / signup surfaces; no intake-contract changes; no sample regeneration. Files changed limited to `run-cppa-risk-assessment/` + docs:
- `supabase/functions/run-cppa-risk-assessment/_w23_risk_turnb.ts` (target-field extension only)
- `supabase/functions/run-cppa-risk-assessment/_w24_risk_turna.ts` (new)
- `supabase/functions/run-cppa-risk-assessment/_w24_risk_turna.test.ts` (new)
- `supabase/functions/run-cppa-risk-assessment/index.ts` (BUILD_STAMP bump `w23…` → `w24-risk-turna@2026-07-25T18:08:00Z`; W24 import + boot-log echo; wire seam AFTER W23 turnB and BEFORE LEAK-PREV P1 emit-gate)
- `docs/courier/W24-RISK-TURNA-2026-07-25.md` (this file)
- `docs/pipeline-state.md` (item 74 + header restamp)

## Queue posture
`W24-ADMT-ATTRIBUTION-FIX` (item 73 queue slot 2) remains **QUEUED, controller-owned** — NOT started this turn per dispatch instruction. Wave-25 expected ~19:45Z (135m spacing from 17:30:02Z launch).
