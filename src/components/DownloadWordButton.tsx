// Word/.docx export has been removed site-wide.
// This stub preserves the import surface used by many pages while rendering nothing.
type Props = {
  text?: string;
  label?: string;
  subtitle?: string;
  className?: string;
  buttonLabel?: string;
};

export default function DownloadWordButton(_props: Props) {
  return null;
}
